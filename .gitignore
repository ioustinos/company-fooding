node_modules
dist
.env.local
.env
.DS_Store

# Local Netlify folder
.netlify

# Temporary
*.log
.cache

# Drag-drop artifact (use public/spec/ instead)
design-spec/orexi-deploy/

# Cross-session memory (contains secrets like github_credentials.sh — NEVER commit)
.auto-memory/

# Generated reports (re-generate from scripts/ when needed)
reports/

# Netlify deploy artifacts (stray zips in repo root)
deploy-*.zip
